name: securely
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  local_auth: ^2.3.0
  path_provider: ^2.1.5
  file_picker: ^10.2.0
  permission_handler: ^12.0.1
  open_file: ^3.3.2
  encrypt: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
